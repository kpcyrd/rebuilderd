[workspace]
members = [
    "common",
    "daemon",
    "tests",
    "tools",
    "worker",
]
resolver = "2"

[workspace.dependencies]
rand = "0.10"
rebuilderd-common = { version = "=0.25.0", path = "common" }
toml = "1"
